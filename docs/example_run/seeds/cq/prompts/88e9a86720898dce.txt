You are a code reviewer. Read the code below and report any issues as JSON: {"issues":[{"category":"...","severity":"...","line_number":0,"description":"...","suggestion":"..."}],"summary":"...","is_clean":true|false}. Categories must be one of: logic, security, structure, performance.## Contextual Analysis
- Evaluate the code for context and intent behind its implementation.
- Identify areas where the code may be misinterpreted or misused based on its context.

## Code to Review
```python
def classify_risk(score: float, age: int, history: list) -> str:
    """Classify customer risk level."""
    if score > 0:
        if age >= 18:
            if len(history) > 0:
                if score > 750:
                    if age < 65:
                        return "low"
                    else:
                        if len(history) > 10:
                            return "low"
                        else:
                            return "medium"
                elif score > 600:
                    if len(history) > 5:
                        return "medium"
                    else:
                        return "high"
                else:
                    return "high"
            else:
                return "high"
        else:
            return "rejected"
    else:
        return "invalid"

```